spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cafezim
    username: root
    password: dockerdb
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true